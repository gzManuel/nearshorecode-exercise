import './Spinner.css';
/**
 * Loader spinner
 */
const Spinner = () => {
    return (
        <div style={{ width: '100%' }} >
            <div className='loader'>

            </div>
        </div>
    );
};

export default Spinner;